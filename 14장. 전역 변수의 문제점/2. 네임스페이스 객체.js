var MYAPP = {}; // 전역 네임스페이스 객체

MYAPP.name = "Jeon";

console.log(MYAPP.name); // JEON


var MYAPP = {}; // 전역 네임스페이스 객체

MYAPP.person = {
    name: "Jeon",
    address: "Seoul"
}

console.log(MYAPP.person.name); // JEON