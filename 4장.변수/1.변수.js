const userId = 1;
const useName = "Soobin";

const user = { id: 1, name: "Soobin" };

const users = [
  {
    id: 1,
    name: "Soobin",
  },
  { id: 2, name: "Jeon" },
];

console.log(score); // undefined

score = 80;
var score;

console.log(score); // 80
